<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MigraineTrackingApp.View.RecordMigraineType"
             BackgroundColor="#003366">


        <StackLayout>
        <StackLayout>
            <ListView x:Name="MigraineTypeListView" ItemsSource="{Binding migraineTypes}" Margin="20" SeparatorVisibility="Default">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="0.50*" />
                                    <ColumnDefinition Width="0.50*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="40" />
                                </Grid.RowDefinitions>
                                <Label Grid.Row="0" Grid.Column="0" 
                                   TextColor="White" 
                                   FontSize="Medium" FontFamily="Arial"  
                                   HorizontalTextAlignment="Center" 
                                   Text="{Binding .}"/>
                                <Button 
                                Grid.Row="0" 
                                Grid.Column="1"
                                BackgroundColor="#53A2F0" 
                                Text="Add" 
                                TextColor="Black" 
                                CornerRadius="10"
                                Clicked="addToList"
                                CommandParameter="{Binding .}"/>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>

                <Entry x:Name="addMigraineType"
               Placeholder="Add Another Migraine/Headache Type"
               PlaceholderColor="White"
               TextColor="White"
               VerticalOptions="Center"/>


            <Button  
                BackgroundColor="#53A2F0" 
                Grid.Row="5" 
                Grid.Column="0" 
                Text="ADD" 
                TextColor="Black" 
                CornerRadius="10"
                Clicked="addToList"/>

        <StackLayout>
            <ListView  HeightRequest="500" x:Name="showListView" ItemsSource="{Binding selectedMigraneTypes}" Margin="20">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="0.50*" />
                                    <ColumnDefinition Width="0.50*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="40" />
                                </Grid.RowDefinitions>
                                <Label Grid.Row="0" Grid.Column="0" 
                                   TextColor="White" 
                                   FontSize="Medium" FontFamily="Arial"  
                                   HorizontalTextAlignment="Center" 
                                   Text="{Binding .}"/>
                                <Button 
                                Grid.Row="0" 
                                Grid.Column="1"
                                BackgroundColor="#53A2F0" 
                                Text="Remove" 
                                TextColor="Black" 
                                CornerRadius="10"
                                Clicked="removeFromList"
                                CommandParameter="{Binding .}"/>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>

        <Button x:Name="backButton" 
                BackgroundColor="#53A2F0" 
                Grid.Row="5" 
                Grid.Column="0" 
                Text="BACK" 
                TextColor="Black" 
                Clicked="returnToMenu"
                CornerRadius="10" />

            <Button x:Name="saveButton" 
                BackgroundColor="#53A2F0" 
                Grid.Row="5"
                Grid.Column="1" 
                Text="SAVE" 
                TextColor="Black" 
                Clicked="saveTypes"
                CornerRadius="10" />

        </StackLayout>
</ContentPage>